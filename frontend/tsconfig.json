{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  /* Added per https://ui.shadcn.com/docs/tailwind-v4 */
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      // instead of 'import { type ApiRoutes } from "../../../server/app"'
      // allows us to do from '@server/app'
      // We also need this in vite.config.ts
      // we may also need this in tsconfig.app.json, not sure, but added it just
      // in case.
      "@server/*": ["../server/*"]
    }
  }
}
